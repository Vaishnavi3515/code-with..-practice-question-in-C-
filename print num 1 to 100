#include <bits/stdc++.h>
using namespace std;
int main(){
    int i=1;
    while(i<=100){
    if(i==101){
     break;
    }
    cout<<i<<endl;
    i++;
    }
cout<<"\n loop terminated using break."<<endl;
    return 0;
}
